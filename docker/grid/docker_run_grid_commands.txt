  #Create a Docker Network
  docker network create jenkinsnet

  #Start the Hub using the created network
  docker run -d -p 4442-4444:4442-4444 --platform linux/amd64 --net jenkinsnet --name selenium-hub selenium/hub:latest

  Start the Node using the created network
  docker run -d --platform linux/amd64 --net jenkinsnet -e SE_EVENT_BUS_HOST=selenium-hub \
      --shm-size="2g" \
      -e SE_EVENT_BUS_PUBLISH_PORT=4442 \
      -e SE_EVENT_BUS_SUBSCRIBE_PORT=4443 \
      selenium/node-chrome:latest

  docker run -d --net jenkinsnet -e SE_EVENT_BUS_HOST=selenium-hub \
      --shm-size="2g" \
      -e SE_EVENT_BUS_PUBLISH_PORT=4442 \
      -e SE_EVENT_BUS_SUBSCRIBE_PORT=4443 \
      selenium/node-edge:latest

  docker run -d --platform linux/amd64 --net jenkinsnet -e SE_EVENT_BUS_HOST=selenium-hub \
      --shm-size="2g" \
      -e SE_EVENT_BUS_PUBLISH_PORT=4442 \
      -e SE_EVENT_BUS_SUBSCRIBE_PORT=4443 \
      selenium/node-firefox:latest